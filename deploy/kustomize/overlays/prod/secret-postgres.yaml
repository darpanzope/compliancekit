# PLACEHOLDER — replace before kubectl apply. Production deployments
# should source the DSN from sealed-secrets / external-secrets /
# SOPS rather than committing the plaintext DSN here.
apiVersion: v1
kind: Secret
metadata:
  name: postgres
type: Opaque
stringData:
  dsn: "postgres://compliancekit:REPLACE_ME@compliancekit-postgres:5432/compliancekit?sslmode=require"
