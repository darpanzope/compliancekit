apiVersion: v1
kind: Service
metadata:
  name: daemon
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: compliancekit
    app.kubernetes.io/component: daemon
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
