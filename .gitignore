# Build artifacts
bin/
dist/
*.test
*.out
coverage.txt
coverage.html

# v1.4 UI build pipeline (per-developer toolchain cache; the standalone
# Tailwind CLI lives here after `make ui-setup`). Compiled assets
# under internal/server/assets/ ARE committed — CI gates on their
# freshness via `make ui-check`.
.cache/

# IDE
.idea/
.vscode/
*.swp
.DS_Store

# State and outputs
.compliancekit/
out/
# Runtime evidence packs (top-level only -- the internal/evidence/
# package containing the pack writer must remain trackable).
/evidence/

# Claude Code session state
.claude/
